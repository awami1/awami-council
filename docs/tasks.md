# المرحلة الأولى — Tasks

### نظام هوية الأعضاء والصفحة الشخصية — alawami.site

> مبني على PRD v1.1 — مارس 2026

-----

## المرحلة أ: قاعدة البيانات (التبعية: لا شيء)

### Task 1: إنشاء ملف migration-phase1.sql

- [ ] إنشاء جدول `member_users` كما في PRD §3.1
- [ ] تعديل جدول `members`: إضافة `منقطع` لقيم ENUM + حقل `status_override` + حقل `status_override_note`
- [ ] تعديل جدول `committee_members`: إضافة `start_year` + `end_year` + `role`
- [ ] إنشاء جدول `member_objections` كما في PRD §3.4
- [ ] اختبار تنفيذ الملف على قاعدة بيانات تجريبية بدون أخطاء

-----

## المرحلة ب: نظام المصادقة — Backend (التبعية: المرحلة أ)

### Task 2: ملف الحماية member-guard.php

- [ ] إنشاء `/api/auth/member-guard.php`
- [ ] دالة `requireMemberAuth()` مع `session_name('awami_member')`
- [ ] التحقق من وجود الجلسة → 401 إذا فارغة
- [ ] التحقق من `expires_at` → تدمير الجلسة إذا منتهية
- [ ] تجديد الصلاحية (24 ساعة) عند كل طلب ناجح
- [ ] إرجاع `member_id` + `awm_id` + `name`

### Task 3: نقطة تسجيل الدخول member-login.php

- [ ] إنشاء `/api/auth/member-login.php`
- [ ] استقبال `awm_id` + `password`
- [ ] التحقق من `is_active = 1`
- [ ] التحقق من كلمة السر بـ `password_verify()`
- [ ] Rate Limiting: 5 محاولات فاشلة → قفل 15 دقيقة
- [ ] إنشاء جلسة `awami_member` مع البيانات المطلوبة
- [ ] تحديث `last_login` في `member_users`
- [ ] إذا `first_login = 1` → إرجاع flag يطلب تغيير كلمة السر
- [ ] تسجيل الدخول في `logAudit()`

### Task 4: نقطة التفعيل activate.php

- [ ] إنشاء `/api/auth/activate.php`
- [ ] استقبال `awm_id` + `temp_token`
- [ ] التحقق من صلاحية الرمز (48 ساعة) عبر `token_expiry`
- [ ] تفعيل الحساب: `is_active = 1`
- [ ] حذف `temp_token` بعد التفعيل
- [ ] إنشاء جلسة عضو مباشرة
- [ ] إرجاع flag `first_login` لإجبار تغيير كلمة السر

### Task 5: نقطة تغيير كلمة السر change-password.php

- [ ] إنشاء `/api/auth/change-password.php`
- [ ] حماية بـ `requireMemberAuth()`
- [ ] استقبال كلمة السر الجديدة (8 أحرف minimum)
- [ ] تشفير بـ `password_hash()` مع BCRYPT
- [ ] تحديث `first_login = 0`
- [ ] تسجيل التغيير في `logAudit()`

### Task 6: نقطة تسجيل الخروج member-logout.php

- [ ] إنشاء `/api/auth/member-logout.php`
- [ ] `session_name('awami_member')` → `session_unset()` → `session_destroy()`
- [ ] إعادة التوجيه لصفحة `/login`

### Task 7: نقطة نسيت كلمة السر forgot-password.php

- [ ] إنشاء `/api/auth/forgot-password.php`
- [ ] استقبال `awm_id` فقط (بدون مصادقة — نقطة عامة)
- [ ] التحقق من وجود الحساب وأنه `is_active = 1`
- [ ] توليد رمز مؤقت جديد (8 أحرف عشوائية) + تحديث `token_expiry` = 48 ساعة
- [ ] إلغاء كلمة السر القديمة + تعيين `first_login = 1`
- [ ] إرسال رسالة واتساب تلقائية للعضو عبر WhatsApp API (الرمز الجديد + رابط الدخول + التعليمات)
- [ ] Rate Limiting: 3 طلبات لكل AWM-ID خلال 24 ساعة (منع الإساءة)
- [ ] إرجاع رسالة عامة “إذا كان الرقم مسجلاً، ستصلك رسالة” (عدم كشف وجود الحساب)
- [ ] تسجيل الطلب في `logAudit()`

### Task 8: CSRF Protection

- [ ] إضافة حماية CSRF على كل نقاط POST الخاصة بالأعضاء

-----

## المرحلة ج: نقاط API للعضو (التبعية: المرحلة ب)

### Task 9: نقطة الصفحة الشخصية profile.php

- [ ] إنشاء `/api/member/profile.php`
- [ ] حماية بـ `requireMemberAuth()`
- [ ] جلب بيانات العضو الأساسية من `members`
- [ ] جلب اللجان الحالية (`end_year IS NULL`) مع الدور
- [ ] جلب اللجان السابقة (`end_year IS NOT NULL`) مع السنوات والدور
- [ ] جلب سجل الاشتراكات (كل الفترات مع حالة الدفع)
- [ ] حساب: مجموع المدفوع / المطلوب / نسبة الالتزام
- [ ] جلب الاعتراضات السابقة مع حالتها ورد المدير
- [ ] إرجاع JSON شامل

### Task 10: نقطة تقديم الاعتراض objection.php

- [ ] إنشاء `/api/member/objection.php`
- [ ] حماية بـ `requireMemberAuth()`
- [ ] استقبال: `subject` + `body` + `related_to` + `related_id` (اختياري)
- [ ] إنشاء سجل في `member_objections` بحالة `جديد`
- [ ] إرسال إيميل تنبيه للمدير (AWM-ID + اسم العضو + الموضوع)
- [ ] إرجاع تأكيد للعضو

### Task 11: نقطة تقرير PDF — report.php

- [ ] إنشاء `/api/member/report.php`
- [ ] حماية بـ `requireMemberAuth()`
- [ ] تثبيت مكتبة mPDF أو TCPDF
- [ ] بناء تقرير PDF يحتوي: شعار المجلس + البيانات الشخصية + اللجان + سجل الاشتراكات + الإجمالي + نسبة الالتزام
- [ ] دعم RTL كامل مع خط Amiri أو Cairo
- [ ] ألوان: الأزرق #1B3456 + الذهبي #c8a84b + الأخضر #47915C
- [ ] تذييل: “صدر من: الموقع الرسمي لعائلة العوامي | هذا التقرير معلوماتي”

-----

## المرحلة د: واجهات العضو — Frontend (التبعية: المرحلة ج)

### Task 12: صفحة تسجيل الدخول /login

- [ ] إنشاء `/login/index.php`
- [ ] نموذج: حقل AWM-ID + حقل كلمة السر / الرمز المؤقت
- [ ] ربط مع `/api/auth/member-login.php` عبر fetch
- [ ] عند `first_login` → توجيه لنموذج تغيير كلمة السر
- [ ] عند النجاح → توجيه لـ `/member`
- [ ] رسائل خطأ واضحة (بيانات خاطئة / حساب غير مفعل / محاولات كثيرة)
- [ ] رابط “نسيت كلمة السر؟” → يظهر حقل AWM-ID فقط → يرسل طلب لـ `/api/auth/forgot-password.php` → رسالة تأكيد “إذا كان الرقم مسجلاً، ستصلك رسالة واتساب”
- [ ] تصميم متوافق مع هوية الموقع (RTL + خطوط + ألوان)
- [ ] Responsive كامل

### Task 13: الصفحة الشخصية /member

- [ ] إنشاء `/member/index.php`
- [ ] التحقق من الجلسة — إعادة توجيه لـ `/login` إذا غير مسجل
- [ ] **أ. بطاقة الهوية**: الاسم الكامل + AWM-ID + تاريخ الانضمام + حالة العضوية (بألوان: نشط/منقطع/غير نشط/معفي)
- [ ] **ب. اللجان الحالية**: قائمة مع الأيقونة + الدور + تاريخ الانضمام
- [ ] **ج. اللجان السابقة**: قائمة مطوية بالافتراضي (أكورديون) مع السنوات والدور
- [ ] **د. سجل الاشتراكات**: جدول بكل الفترات + ملخص أعلى (مجموع مدفوع / مطلوب / نسبة التزام)
- [ ] **هـ. إجراءات العضو**: زر طباعة التقرير PDF + زر تقديم ملاحظة/اعتراض
- [ ] **و. اعتراضاتي السابقة**: جدول بالموضوع + الحالة + رد المدير
- [ ] نموذج تقديم اعتراض (modal): نوع + موضوع + تفاصيل
- [ ] Responsive كامل + RTL

-----

## المرحلة هـ: لوحة التحكم — إضافات الأدمن (التبعية: المرحلة ب)

### Task 14: وظائف تفعيل الحسابات وإعادة تعيين كلمة السر admin-members-auth.js

- [ ] إنشاء `/admin/js/admin-members-auth.js`
- [ ] زر “تفعيل حساب” على كل سجل عضو (يظهر فقط إذا ما عنده حساب)
- [ ] عند الضغط: توليد AWM-ID تلقائي (تسلسلي 4 أرقام) + رمز مؤقت (8 أحرف عشوائية)
- [ ] إنشاء سجل في `member_users` مع `token_expiry` = 48 ساعة
- [ ] عرض نافذة تحتوي رسالة واتساب جاهزة للنسخ (الاسم + AWM-ID + الرمز + الرابط + التعليمات)
- [ ] حفظ بيانات التفعيل في قاعدة البيانات
- [ ] زر “إعادة تعيين كلمة السر” (يظهر فقط إذا الحساب مُفعَّل مسبقاً)
- [ ] عند الضغط: توليد رمز مؤقت جديد + إلغاء كلمة السر القديمة + `first_login = 1`
- [ ] عرض نافذة واتساب جاهزة بنص مخصص لإعادة التعيين

### Task 15: أعمدة جديدة في جدول الأعضاء (لوحة التحكم)

- [ ] إضافة عمود “AWM-ID” في جدول الأعضاء
- [ ] إضافة عمود “حالة الحساب”: مُفعَّل / غير مُفعَّل / آخر دخول
- [ ] فلترة إضافية حسب حالة الحساب

### Task 16: تبويب الاعتراضات في قسم الرسائل

- [ ] إضافة تبويب “الاعتراضات” مستقل عن رسائل التواصل
- [ ] فلترة حسب الحالة: جديد / قيد المراجعة / تمت المعالجة / مرفوض
- [ ] نافذة تفاصيل الاعتراض: عرض البيانات + تغيير الحالة + كتابة رد
- [ ] حفظ الرد في `admin_reply` + تحديث `replied_at` + تحديث الحالة

### Task 17: قسم “مراجعة الحالات”

- [ ] قسم يظهر عند إنشاء فترة مالية جديدة
- [ ] حساب حالة كل عضو تلقائياً بناءً على سجل دفعاته (§4.1)
- [ ] مقارنة الحالة المحسوبة بالحالة الحالية
- [ ] عرض قائمة التغييرات المقترحة: الحالة الحالية ← المقترحة
- [ ] تجاهل الأعضاء بـ `status_override = 1`
- [ ] أزرار: “تأكيد الكل” أو مراجعة فردية مع إمكانية التجاوز اليدوي + كتابة السبب

-----

## المرحلة و: اختبار ومراجعة (التبعية: كل ما سبق)

### Task 18: اختبار شامل

- [ ] تفعيل حساب عضو من لوحة التحكم → التحقق من توليد AWM-ID والرمز
- [ ] تسجيل دخول بالرمز المؤقت → إجبار تغيير كلمة السر → دخول الصفحة الشخصية
- [ ] التحقق من انتهاء صلاحية الرمز بعد 48 ساعة
- [ ] التحقق من Rate Limiting (5 محاولات فاشلة)
- [ ] نسيت كلمة السر: طلب من صفحة الدخول → وصول رسالة واتساب → دخول بالرمز الجديد → تغيير كلمة السر
- [ ] نسيت كلمة السر: التحقق من Rate Limiting (3 طلبات / 24 ساعة لكل AWM-ID)
- [ ] إعادة تعيين كلمة السر من لوحة التحكم (زر المدير) → التحقق من إلغاء كلمة السر القديمة
- [ ] عرض بيانات الصفحة الشخصية بشكل صحيح (لجان + اشتراكات + اعتراضات)
- [ ] تقديم اعتراض → ظهوره في لوحة التحكم → رد المدير → ظهور الرد للعضو
- [ ] طباعة تقرير PDF بالعربية RTL
- [ ] حساب الحالة التلقائي عند إنشاء فترة جديدة
- [ ] فصل جلسات الأدمن عن الأعضاء (تسجيل دخول متزامن لا يتعارض)
- [ ] اختبار على الموبايل (Responsive)

-----

## ملاحظات التنفيذ

- **الترتيب**: المراحل (أ → ب → ج → د → هـ) تعتمد على بعضها بالتسلسل
- **المراحل د و هـ** يمكن العمل عليها بالتوازي بعد إكمال ج
- **Claude Code**: كل Task مصمم ليكون prompt مستقل قابل للتنفيذ في جلسة واحدة
- **الريبو**: github.com/awami1/awami-council
