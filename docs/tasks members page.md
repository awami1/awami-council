# تحسين صفحة الأعضاء — Tasks

### لوحة تحكم مجلس عائلة العوامي — alawami.site

> مبني على PRD v1.0 — مارس 2026

-----

## المرحلة أ: تغيير حالات العضوية (التبعية: لا شيء)

### Task 1: تعديل قاعدة البيانات — migration

- [ ] نسخة احتياطية من قاعدة البيانات قبل أي تعديل
- [ ] ترحيل بيانات members: `UPDATE members SET status = 'مشترك' WHERE status = 'نشط'`
- [ ] ترحيل بيانات members: `UPDATE members SET status = 'غير مشترك' WHERE status = 'غير نشط'`
- [ ] ترحيل بيانات members: `UPDATE members SET status = 'مشترك' WHERE status = 'معفي'`
- [ ] MySQL: تعديل ENUM لعمود status: `ENUM('مشترك','منقطع','غير مشترك') DEFAULT 'مشترك'`
- [ ] SQLite: تعديل CHECK constraint بنفس القيم
- [ ] ترحيل بيانات payments: `UPDATE payments SET status = 'لم يدفع' WHERE status = 'معفي'`
- [ ] اختبار تنفيذ الـ migration على قاعدة بيانات تجريبية بدون أخطاء

### Task 2: تحديث Backend — members.php

- [ ] تعديل ENUM/CHECK في `handlePost` — القيمة الافتراضية `'مشترك'`
- [ ] تعديل `$allowedStatus` في `handleApplyStatuses`: `['مشترك', 'منقطع', 'غير مشترك']`
- [ ] تحديث `handleReviewStatuses` — قواعد الحساب الجديدة:
  - [ ] مشترك: دفع آخر فترتين (unpaidStreak = 0 أو 1)
  - [ ] منقطع: انقطاع فترتين بالضبط (unpaidStreak = 2)
  - [ ] غير مشترك: انقطاع أكثر من فترتين (unpaidStreak > 2)
- [ ] إزالة أي منطق خاص بحالة “معفي” في `handleReviewStatuses`
- [ ] إزالة شرط `$m['status'] === 'معفي'` من التجاهل في مراجعة الحالات

### Task 3: تحديث Frontend — كامل النظام

- [ ] `modals.php` — modal-member: تعديل select الحالة إلى مشترك / منقطع / غير مشترك
- [ ] `modals.php` — modal-pay: إزالة خيار “معفي” من select حالة الدفع
- [ ] `admin-app.js` — `renderMembers`: تحديث badges الحالة:
  - [ ] مشترك: `badge-success` أخضر
  - [ ] منقطع: `badge-warning` برتقالي
  - [ ] غير مشترك: `badge-gray` رمادي
- [ ] `admin-app.js` — `renderMembers`: تحديث فلتر الحالة `m-flt-status`
- [ ] `admin-app.js` — `renderFees`: إزالة badge وإحصائيات المعفيين
- [ ] `admin-app.js` — `renderDashboard`: إزالة عداد المعفيين
- [ ] `admin-app.js` — `saveMember`: التأكد من القيم الجديدة
- [ ] `state.js` — `saveMember`: القيمة الافتراضية `'مشترك'` بدل `'نشط'`
- [ ] `state.js` — `createPeriod`: إزالة منطق “إذا العضو معفي يسجل كمعفي”
- [ ] `admin-import.js` — القيمة الافتراضية `'مشترك'` بدل `'نشط'`
- [ ] `admin-members-auth.js` — تحديث أي إشارة للحالات القديمة
- [ ] بحث شامل `grep -r "نشط\|معفي\|غير نشط"` في كل ملفات JS و PHP — التأكد من عدم وجود بقايا
- [ ] `index.php` — تحديث فلاتر الحالة في HTML صفحة الأعضاء

-----

## المرحلة ب: إصلاح زر الحفظ (التبعية: لا شيء)

### Task 4: معالجة تعليق الطلبات المكررة — index.php

- [ ] إضافة `DEDUP_TIMEOUT = 10000` (10 ثواني) لحذف الطلبات المعلقة من `_pending`
- [ ] إضافة `setTimeout` بعد `_pending.set()` لحذف المفتاح بعد الـ timeout
- [ ] التأكد أن `.finally()` لا يزال يحذف المفتاح عند الاكتمال الطبيعي
- [ ] اختبار: ضغط زر الحفظ مرتين متتاليتين — الأول يمر والثاني ينتظر ثم يُسمح بعد الـ timeout
- [ ] إضافة حالة loading على زر الحفظ في `saveMember()`: spinner + تعطيل الزر
- [ ] إعادة تفعيل الزر بعد نجاح أو فشل الطلب (في `.then()` و `.catch()`)
- [ ] تطبيق نفس نمط loading على أزرار الحفظ الأخرى (modal-pay, modal-period, modal-tx)

-----

## المرحلة ج: إصلاح الاستيراد من Excel (التبعية: المرحلة أ)

### Task 5: إعادة كتابة منطق الاستيراد — admin-import.js

- [ ] إزالة `State.getMembers().push(newMember)` بالكامل
- [ ] إزالة `saveDB()` بالكامل من ملف الاستيراد
- [ ] بعد قراءة الملف ومعالجته: تخزين النتائج في مصفوفة مؤقتة `pendingImport[]`
- [ ] كل سجل في المصفوفة يحتوي: `{ name, phone, status, type: 'new'|'update'|'skip', existingId?, skipReason? }`
- [ ] `findMember()` يبقى كما هو لكشف الأعضاء الموجودين
- [ ] `isValidName()` يبقى كما هو لفلترة السجلات غير الصالحة
- [ ] القيمة الافتراضية لحالة العضو الجديد: `'مشترك'`

### Task 6: إضافة خطوة المراجعة — جدول المعاينة

- [ ] بعد المعالجة: عرض جدول بكل السجلات في `#import-result`
- [ ] أعمدة الجدول: # / الاسم / الجوال / النوع (جديد/تحديث/متجاهل) / إجراء
- [ ] تمييز الأعضاء الجدد: خلفية خضراء فاتحة + badge “جديد”
- [ ] تمييز الأعضاء الموجودين (تحديث): خلفية صفراء فاتحة + badge “تحديث”
- [ ] تمييز المتجاهلين: خلفية رمادية + badge “متجاهل” مع سبب التجاهل
- [ ] زر حذف (✕) على كل سجل جديد/تحديث — يحذفه من `pendingImport[]`
- [ ] إمكانية تعديل الاسم والجوال مباشرة في الجدول (contenteditable أو input)
- [ ] عرض ملخص أعلى الجدول: `جديد: X | تحديث: X | متجاهل: X | الإجمالي: X`

### Task 7: حفظ الاستيراد عبر API

- [ ] زر “حفظ الكل” في footer نافذة `modal-import-excel` — يظهر فقط بعد المعالجة
- [ ] عند الضغط: فلترة `pendingImport[]` — فقط السجلات من نوع `'new'` و `'update'`
- [ ] لكل عضو جديد: `POST /api/members.php` عبر `apiFetch` (مع CSRF)
- [ ] لكل عضو موجود: `PUT /api/members.php?id=X` عبر `apiFetch`
- [ ] تحويل البيانات من camelCase إلى snake_case قبل الإرسال (name, phone, status, join_date)
- [ ] عرض شريط تقدم (progress bar) أثناء الحفظ — يتحدث مع كل طلب مكتمل
- [ ] معالجة الأخطاء: إذا فشل طلب → عرض اسم العضو + رسالة الخطأ + الاستمرار بالباقي
- [ ] بعد اكتمال الحفظ: عرض ملخص نهائي (نجح: X / فشل: X)
- [ ] استدعاء `MemberService.syncMembersFromAPI()` لتحديث الـ State cache
- [ ] استدعاء `renderMembers()` لتحديث الجدول
- [ ] تسجيل العملية في `logAudit()` — “استيراد X عضو من Excel”
- [ ] زر “إلغاء” — يمسح `pendingImport[]` ويخفي جدول المراجعة
- [ ] حالة loading على زر الحفظ: spinner + تعطيل أثناء الإرسال

### Task 8: تحسين نافذة الاستيراد — modals.php

- [ ] تعديل HTML نافذة `modal-import-excel`:
  - [ ] إضافة `<div id="import-review-table">` لجدول المراجعة
  - [ ] إضافة progress bar: `<div id="import-progress">` مخفي افتراضياً
  - [ ] تعديل footer: إضافة زر “حفظ الكل” (مخفي افتراضياً) + إبقاء زر “إغلاق”
- [ ] حالة loading على زر “حفظ الكل”: `setBtnLoading()` الموجودة في admin-core.js

-----

## المرحلة د: تحسين نافذة إضافة العضو (التبعية: المرحلة أ)

### Task 9: إزالة الحقول غير المطلوبة

- [ ] `modals.php` — إزالة div حقل “رقم الهوية” (`mm-idnum`) من HTML النافذة
- [ ] `modals.php` — إزالة div حقل “الفرع العائلي” (`mm-family`) من HTML النافذة
- [ ] `admin-app.js` — `clearMemberForm()`: إزالة `'mm-idnum'` و `'mm-family'` من المصفوفة
- [ ] `admin-app.js` — `saveMember()`: إزالة قراءة `mm-idnum` و `mm-family` من data
- [ ] `admin-app.js` — `editMember()`: إزالة تعبئة `mm-idnum` و `mm-family`
- [ ] التأكد أن الحقول تبقى في قاعدة البيانات ولا تُحذف — فقط تُخفى من الواجهة
- [ ] التأكد أن API `handlePost` و `handlePut` لا يتأثران — الحقول اختيارية أصلاً

### Task 10: تحسين التحقق من البيانات — validation

- [ ] إنشاء دالة `validateMemberForm()` جديدة في `admin-app.js` تستبدل التحقق الحالي
- [ ] **الاسم — مطلوب + ثلاثي:**
  - [ ] فصل الاسم بالمسافات: `name.trim().split(/\s+/)`
  - [ ] التحقق أن عدد الكلمات ≥ 3
  - [ ] رسالة: “يجب إدخال الاسم الثلاثي على الأقل”
  - [ ] أمثلة: ❌ “علي العوامي” (ثنائي) / ✅ “علي عماد العوامي” (ثلاثي)
- [ ] **الاسم — كشف التكرار:**
  - [ ] فحص ضد `State.getMembers()` — مطابقة بعد `trim().toLowerCase()`
  - [ ] استثناء العضو الحالي عند التعديل (مقارنة بالـ id)
  - [ ] عند التكرار: عرض تنبيه “يوجد عضو بنفس الاسم — هل تريد المتابعة؟” مع زر متابعة
- [ ] **الجوال — صيغة:**
  - [ ] إذا أُدخل قيمة: التحقق بـ `/^05\d{8}$/` (10 أرقام تبدأ بـ 05)
  - [ ] إذا فارغ: يُقبل (الحقل اختياري)
  - [ ] رسالة: “صيغة رقم الجوال غير صحيحة — يجب أن يبدأ بـ 05 ويكون 10 أرقام”
- [ ] **الجوال — كشف التكرار:**
  - [ ] فحص ضد `State.getMembers()` — مطابقة بعد إزالة المسافات
  - [ ] استثناء العضو الحالي عند التعديل
  - [ ] رسالة: “يوجد عضو بنفس رقم الجوال”
- [ ] **عرض الأخطاء:**
  - [ ] رسالة خطأ تحت كل حقل (إضافة `<div class="field-error">` بعد كل input)
  - [ ] تلوين الحقل بحدود حمراء (`border-color: var(--danger)`)
  - [ ] مسح الأخطاء عند بداية كل محاولة حفظ
- [ ] **تعديل `saveMember()`:**
  - [ ] استبدال `validateRequired('mm-name')` بـ `validateMemberForm()`
  - [ ] إذا فيه تكرار + المستخدم لم يؤكد المتابعة → return
  - [ ] إذا فيه أخطاء إلزامية → return

-----

## المرحلة هـ: تحسين الجدول والبحث (التبعية: المرحلة أ + د)

### Task 11: تحسين شكل الجدول — admin-app.js + admin.css

- [ ] `renderMembers()` — زيادة padding على خلايا الجدول
- [ ] تحسين عرض badges الحالة — أحجام وألوان واضحة:
  - [ ] مشترك: خلفية `#dcfce7` + نص `#166534`
  - [ ] منقطع: خلفية `#fef9c3` + نص `#854d0e`
  - [ ] غير مشترك: خلفية `#f3f4f6` + نص `#6b7280`
- [ ] تحسين عرض badges الدفع — نفس النمط
- [ ] تحسين أزرار الإجراءات — spacing أفضل + tooltips
- [ ] إضافة hover effect على صفوف الجدول: `tr:hover { background: var(--bg-alt) }`
- [ ] تحسين mobile-cards view:
  - [ ] التأكد أن كل `data-label` محدث بالمسميات الجديدة
  - [ ] تحسين ترتيب البيانات في عرض البطاقات
- [ ] تحسين عرض عمود اللجان — tooltip إذا النص طويل بدل قطعه

### Task 12: تحسين البحث والفلترة — admin-app.js + index.php

- [ ] تحديث خيارات فلتر الحالة في HTML: مشترك / منقطع / غير مشترك
- [ ] إضافة زر “إعادة ضبط” بجانب الفلاتر:
  - [ ] يمسح حقل البحث
  - [ ] يعيد كل الفلاتر للقيمة الافتراضية (الكل)
  - [ ] يستدعي `renderMembers()`
- [ ] عرض عدد النتائج أثناء الفلترة بوضوح: `"عرض X من أصل Y عضو"`
- [ ] تحسين debounce — تقليل الوقت من الحالي إلى 200ms
- [ ] تحسين مظهر شريط البحث:
  - [ ] إضافة أيقونة بحث (🔍) داخل الحقل
  - [ ] حدود أوضح + border-radius متسق
- [ ] تحسين مظهر select الفلاتر — توحيد الحجم والستايل مع شريط البحث

-----

## المرحلة و: اختبار شامل (التبعية: كل ما سبق)

### Task 13: اختبار شامل

**حالات العضوية:**

- [ ] الحالات الجديدة (مشترك / منقطع / غير مشترك) تظهر في نافذة إضافة العضو
- [ ] الحالات الجديدة تظهر في فلتر الحالة
- [ ] الـ badges بالألوان الصحيحة في جدول الأعضاء
- [ ] حالة “معفي” غير موجودة في أي مكان — بحث شامل
- [ ] قواعد الحساب التلقائي تعمل بالحالات الجديدة عند إنشاء فترة مالية
- [ ] البيانات المُرحّلة صحيحة (نشط → مشترك، غير نشط → غير مشترك)

**زر الحفظ:**

- [ ] إضافة عضو جديد — الزر يعمل من أول ضغطة
- [ ] تعديل عضو — الزر يعمل
- [ ] الضغط مرتين سريعاً — لا يُنشئ عضوين
- [ ] حالة loading (spinner) تظهر أثناء الحفظ
- [ ] بعد فشل الشبكة — الزر يعمل مرة ثانية بعد 10 ثواني

**الاستيراد من Excel:**

- [ ] رفع ملف Excel — تظهر خطوة المراجعة (جدول) قبل الحفظ
- [ ] تمييز الجدد بالأخضر والمحدثين بالأصفر والمتجاهلين بالرمادي
- [ ] حذف سجل من جدول المراجعة قبل الحفظ
- [ ] تعديل اسم أو جوال في جدول المراجعة
- [ ] الضغط على “حفظ الكل” — شريط التقدم يظهر
- [ ] البيانات تبقى بعد تحديث الصفحة (محفوظة في MySQL)
- [ ] البيانات تظهر عند الدخول من جهاز آخر
- [ ] `saveDB()` غير مُستدعى في admin-import.js

**نافذة إضافة العضو:**

- [ ] حقلا رقم الهوية والفرع العائلي غير ظاهرين
- [ ] إدخال اسم ثنائي “علي العوامي” → رسالة خطأ واضحة
- [ ] إدخال اسم ثلاثي “علي عماد العوامي” → يُقبل
- [ ] إدخال جوال بصيغة خاطئة “012345” → رسالة خطأ
- [ ] إدخال جوال بصيغة صحيحة “0501234567” → يُقبل
- [ ] إدخال اسم عضو موجود → تنبيه تكرار مع خيار المتابعة
- [ ] إدخال جوال عضو موجود → تنبيه تكرار

**الجدول والبحث:**

- [ ] شكل الجدول العام محسّن — padding + hover + badges
- [ ] زر إعادة ضبط الفلاتر يعمل
- [ ] عدد النتائج يظهر أثناء الفلترة
- [ ] العرض على الموبايل سليم (mobile-cards)
- [ ] البحث يعمل بسرعة مع debounce

-----

## ملاحظات التنفيذ

- **الترتيب**: أ → ب → ج → د → هـ → و
- **المراحل أ و ب** مستقلة — يمكن تنفيذها بالتوازي
- **المرحلة أ** (تغيير الحالات) مطلوبة قبل ج و د و هـ
- كل Task مصمم ليكون **prompt مستقل** قابل للتنفيذ في جلسة واحدة مع Claude Code
- **الريبو**: github.com/awami1/awami-council
